package com.dobemcontabilidade.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ClasseCnaeAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClasseCnaeAllPropertiesEquals(ClasseCnae expected, ClasseCnae actual) {
        assertClasseCnaeAutoGeneratedPropertiesEquals(expected, actual);
        assertClasseCnaeAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClasseCnaeAllUpdatablePropertiesEquals(ClasseCnae expected, ClasseCnae actual) {
        assertClasseCnaeUpdatableFieldsEquals(expected, actual);
        assertClasseCnaeUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClasseCnaeAutoGeneratedPropertiesEquals(ClasseCnae expected, ClasseCnae actual) {
        assertThat(expected)
            .as("Verify ClasseCnae auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClasseCnaeUpdatableFieldsEquals(ClasseCnae expected, ClasseCnae actual) {
        assertThat(expected)
            .as("Verify ClasseCnae relevant properties")
            .satisfies(e -> assertThat(e.getCodigo()).as("check codigo").isEqualTo(actual.getCodigo()))
            .satisfies(e -> assertThat(e.getDescricao()).as("check descricao").isEqualTo(actual.getDescricao()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClasseCnaeUpdatableRelationshipsEquals(ClasseCnae expected, ClasseCnae actual) {
        assertThat(expected)
            .as("Verify ClasseCnae relationships")
            .satisfies(e -> assertThat(e.getGrupo()).as("check grupo").isEqualTo(actual.getGrupo()));
    }
}
