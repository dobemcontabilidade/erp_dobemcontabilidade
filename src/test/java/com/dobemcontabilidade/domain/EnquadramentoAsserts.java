package com.dobemcontabilidade.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class EnquadramentoAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEnquadramentoAllPropertiesEquals(Enquadramento expected, Enquadramento actual) {
        assertEnquadramentoAutoGeneratedPropertiesEquals(expected, actual);
        assertEnquadramentoAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEnquadramentoAllUpdatablePropertiesEquals(Enquadramento expected, Enquadramento actual) {
        assertEnquadramentoUpdatableFieldsEquals(expected, actual);
        assertEnquadramentoUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEnquadramentoAutoGeneratedPropertiesEquals(Enquadramento expected, Enquadramento actual) {
        assertThat(expected)
            .as("Verify Enquadramento auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEnquadramentoUpdatableFieldsEquals(Enquadramento expected, Enquadramento actual) {
        assertThat(expected)
            .as("Verify Enquadramento relevant properties")
            .satisfies(e -> assertThat(e.getNome()).as("check nome").isEqualTo(actual.getNome()))
            .satisfies(e -> assertThat(e.getSigla()).as("check sigla").isEqualTo(actual.getSigla()))
            .satisfies(e -> assertThat(e.getLimiteInicial()).as("check limiteInicial").isEqualTo(actual.getLimiteInicial()))
            .satisfies(e -> assertThat(e.getLimiteFinal()).as("check limiteFinal").isEqualTo(actual.getLimiteFinal()))
            .satisfies(e -> assertThat(e.getDescricao()).as("check descricao").isEqualTo(actual.getDescricao()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEnquadramentoUpdatableRelationshipsEquals(Enquadramento expected, Enquadramento actual) {}
}
