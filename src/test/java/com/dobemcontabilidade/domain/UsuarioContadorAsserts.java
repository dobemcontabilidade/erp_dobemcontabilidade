package com.dobemcontabilidade.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class UsuarioContadorAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertUsuarioContadorAllPropertiesEquals(UsuarioContador expected, UsuarioContador actual) {
        assertUsuarioContadorAutoGeneratedPropertiesEquals(expected, actual);
        assertUsuarioContadorAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertUsuarioContadorAllUpdatablePropertiesEquals(UsuarioContador expected, UsuarioContador actual) {
        assertUsuarioContadorUpdatableFieldsEquals(expected, actual);
        assertUsuarioContadorUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertUsuarioContadorAutoGeneratedPropertiesEquals(UsuarioContador expected, UsuarioContador actual) {
        assertThat(expected)
            .as("Verify UsuarioContador auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertUsuarioContadorUpdatableFieldsEquals(UsuarioContador expected, UsuarioContador actual) {
        assertThat(expected)
            .as("Verify UsuarioContador relevant properties")
            .satisfies(e -> assertThat(e.getEmail()).as("check email").isEqualTo(actual.getEmail()))
            .satisfies(e -> assertThat(e.getSenha()).as("check senha").isEqualTo(actual.getSenha()))
            .satisfies(e -> assertThat(e.getToken()).as("check token").isEqualTo(actual.getToken()))
            .satisfies(e -> assertThat(e.getDataHoraAtivacao()).as("check dataHoraAtivacao").isEqualTo(actual.getDataHoraAtivacao()))
            .satisfies(e -> assertThat(e.getDataLimiteAcesso()).as("check dataLimiteAcesso").isEqualTo(actual.getDataLimiteAcesso()))
            .satisfies(e -> assertThat(e.getSituacao()).as("check situacao").isEqualTo(actual.getSituacao()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertUsuarioContadorUpdatableRelationshipsEquals(UsuarioContador expected, UsuarioContador actual) {
        assertThat(expected)
            .as("Verify UsuarioContador relationships")
            .satisfies(e -> assertThat(e.getContador()).as("check contador").isEqualTo(actual.getContador()))
            .satisfies(e -> assertThat(e.getAdministrador()).as("check administrador").isEqualTo(actual.getAdministrador()));
    }
}
